// put in custom jquery functions here

define ([
    // These first two options are no longer needed because they were added as dependencies for methods in the config.js file
        // 'jquery',
        // 'bootstrap',
    'methods'
    ], function ($, methods) {
        // use custom jQuery 
        // with custom methods for jQuery here
        //console.log('initBootstrap was executed with jquery and methods. bs4 should be available now as well');
    });